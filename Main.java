public class Main {
    public static void main(String[] args) {
        Compiler compiler = new Compiler();
        // Método para compilar o arquivo com a referência do caminho dele
        compiler.compile("Input.txt");
    }
}
